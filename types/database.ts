// Generated types will replace this once Phase 2 migrations are applied
// (via `supabase gen types typescript`). Kept minimal for now so the
// Supabase client helpers have a concrete generic to compile against.
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
